// Здесь я учился рабоать с классами, и в дальнейшем приходилось использовать factor, для красивого и читаемого кода
public class fact {
	public static void main(String[] args) {} //В каждой программе для джавы должен быть метод main, и тут он тоже есть
	public static void factor(int fac) { //Собственно метод фактор, который принимает значение и выдает его факториал
		long result = 1L; //Отсчет я начинаю с цыфры 1, хз почему. L-стоит для получения больших числе чем ~300к
		if (fac >0) { // Проверка на положительное число полученного значения
			for (int x=1;x<=fac;x++) { //Цикл который считает факториал, пока не дойдет до конца
				result = result*x; //С каждым "Кругом" цикла, число умножаеться само на себя (Где то тут у меня закрались подозрения, что я что то делаю не так)
				}
				System.out.printf("%s Факториал числа %s\n", result, fac); //Вывод результата в консоль
			} else {
				System.out.printf("%s меньше нуля, так нельзя", fac); //Вывод в консоль, если полученное число меньше 0
		}
	}
}
